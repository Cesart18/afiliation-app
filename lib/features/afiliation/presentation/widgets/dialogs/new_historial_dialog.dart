import 'package:flutter/material.dart';

class NewHistorialDialog extends StatelessWidget {
  const NewHistorialDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return AlertDialog(
      
    );
  }
}